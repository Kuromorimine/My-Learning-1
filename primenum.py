#inputค่าแล้วให้เช็คว่าเป็นจำนวนเฉพาะมั้ย
x=int(input("เช็คเลขจำนวนเฉพาะ :"))
z=0
while z==0:
    if x == 2*(int(x*0.5)):#จำนวนคู่
        if x/2 != 1:
            print("ไม่เป็น")
            z=1
        if x/2 == 1:
            print("เป็น")
            z=1
# #inputค่าแล้วให้เช็คว่าเป็นจำนวนเฉพาะมั้ย
# x=int(input("เช็คเลขจำนวนเฉพาะ"))
# z=0
# while z==0:
#     if 

# print(int(5/3))